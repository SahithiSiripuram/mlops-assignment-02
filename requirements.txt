# Runtime dependencies for the inference service (what ships inside the Docker image).
# Every key ML library is pinned to an exact version so the image is reproducible.
# Install CPU-only torch with:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt

# --- model runtime ---
torch==2.13.0
torchvision==0.28.0
scikit-learn==1.9.0        # needed to deserialize the logistic-regression baseline
joblib==1.5.3
numpy==2.5.2
pillow==12.3.0

# --- web service ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
python-multipart==0.0.32   # multipart/form-data image uploads

# --- configuration & monitoring ---
PyYAML==6.0.3
prometheus-client==0.26.0
