# Training, experiment tracking, data versioning and test tooling.
# Not installed into the runtime image -- see requirements.txt for that.

-r requirements.txt

# --- experiment tracking & data versioning ---
mlflow==3.15.1
dvc==3.67.1

# --- training & analysis ---
matplotlib==3.11.1
seaborn==0.13.2
pandas==2.3.3
tqdm==4.70.0

# --- tests & linting ---
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1              # FastAPI TestClient transport
requests==2.34.2           # smoke tests / monitoring scripts
ruff==0.16.3
